Write a program that takes as input the size of the array and the elements in the array.
The program then ask the user to enter a particular index and prints the element at that index.
this program may generate Array Index Out Of Bounds Exception.
Use exception handling mechanism to handle this exception.
In the catch block, print the class name of the exception throwm.
